import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF2A2A2A);
  static const Color secondary = Color(0xFF484444);

  // Text Colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;

  // Background Overlay
  static const Color overlay = Colors.black54;

  // Error Colors
  static const Color error = Color.fromARGB(255, 105, 103, 103);
}
